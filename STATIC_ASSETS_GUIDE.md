# 静态资源使用指南

## 📁 静态文件目录说明

项目中有两个目录可以存放静态资源（如图片）：

### 1. `public/images/` 目录（推荐）

**特点：**
- 文件会被直接复制到构建输出目录，不经过 Vite 处理
- 使用绝对路径引用
- 适合大文件或不需要优化的资源

**使用方法：**
```javascript
// 在 Vue 组件或 JS 文件中
image: '/images/ai-research.jpg'

// 在模板中
<img src="/images/ai-research.jpg" alt="研究中心">
```

**目录结构示例：**
```
public/
  └── images/
      ├── ai-research.jpg
      ├── bigdata-center.jpg
      └── cybersecurity.jpg
```

---

### 2. `src/assets/images/` 目录

**特点：**
- 文件会被 Vite 处理和优化
- 需要使用 import 导入或特殊语法
- 适合需要优化的小图片

**使用方法：**
```javascript
// 方法1：在脚本中导入
import aiImage from '@/assets/images/ai-research.jpg'

// 方法2：在模板中使用（需要构建工具支持）
<img :src="require('@/assets/images/ai-research.jpg')" alt="研究中心">
```

---

## 🎯 如何更换研究中心图片

### 步骤 1：准备图片
将你的图片文件放到 `public/images/` 目录下，例如：
- `public/images/ai-center.jpg`
- `public/images/bigdata-center.jpg`
- `public/images/security-center.jpg`
- 等等...

### 步骤 2：修改数据文件
编辑 `src/data/research.js` 文件，更新图片路径：

```javascript
export const researchCenters = [
  {
    id: 1,
    title: '人工智能研究中心',
    // 修改这里：从外部链接改为本地图片
    image: '/images/ai-center.jpg',
    // ... 其他配置
  },
  {
    id: 2,
    title: '大数据研究中心',
    image: '/images/bigdata-center.jpg',
    // ... 其他配置
  },
  // ... 其他研究中心
]
```

---

## 💡 推荐实践

1. **使用 `public/images/` 存放研究中心图片**（最简单）
   - 路径格式：`/images/文件名.jpg`
   - 无需导入，直接引用

2. **图片命名规范**
   - 使用小写字母和连字符
   - 例如：`ai-research-center.jpg`、`big-data-center.jpg`

3. **图片尺寸建议**
   - 建议尺寸：800x600 像素
   - 格式：JPG 或 PNG
   - 文件大小：控制在 500KB 以内

---

## 📝 完整示例

假设你有以下图片文件：
```
public/
  └── images/
      ├── hero-banner.jpg        (首页横幅)
      ├── ai-center.jpg         (人工智能中心)
      ├── bigdata-center.jpg    (大数据中心)
      └── security-center.jpg   (网络安全中心)
```

在 `src/data/research.js` 中使用：
```javascript
{
  id: 1,
  title: '人工智能研究中心',
  image: '/images/ai-center.jpg',  // ← 这样引用
}
```

就这么简单！ 🎉
